import { getByTitle } from "@testing-library/react";

function Info(props) {
/**
 * title
 * desc
 * copyright
 * date
 */
const {title, desc, copyright, date } = props;

    return null;
};

export default Info;